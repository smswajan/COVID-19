import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <h2>May Allah (SWT) Protect Us</h2>
        </header>
    );
};

export default Header;